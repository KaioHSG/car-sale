{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "node-terminal",
            "request": "launch",
            "name": "Launch Page",
            "command": "npm run dev",
            "cwd": "${workspaceFolder}",
        }
    ]
}
